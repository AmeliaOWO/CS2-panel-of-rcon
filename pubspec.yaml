name: cs2_rcon_panel
description: CS2 Server RCON Control Panel - Cross-platform desktop & mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  connectivity_plus: ^6.1.3
  google_fonts: ^6.2.1
  # 连接历史通过 dart:io 文件存储，无需额外依赖

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
